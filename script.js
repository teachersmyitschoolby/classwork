// git init 

// git add .
// git commit -am 'комментарий'
// git push origin [ветка]
// git checkout -b Yauheni



function test (a,b) {
    return a+b
}

function sum (a, b) {
    return a + b;
}

const NumberDivide = (a, b) => a / b;

